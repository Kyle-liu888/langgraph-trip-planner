**Navicat 都运行在 Windows 上。** 你的两个数据库都是 PostgreSQL：一个在本机 Docker 容器里，另一个在阿里云服务器的 Docker 容器里。它们的数据、密码彼此独立。

按下面步骤，在 Navicat 中分别建立两个连接。

**先连接本地数据库。**

我检查到本地数据库目前已停止。先打开 Docker Desktop，等待启动完成，然后在 **Windows PowerShell** 执行：

```powershell
wsl -d Ubuntu -- docker start local-dev-postgres
```

仅查看数据库不需要启动前端、后端，也不需要激活 Python 虚拟环境。

打开 Navicat，点击 **“连接 → PostgreSQL”**，在“常规”页填写：

| 配置项 | 填写内容 |
|---|---|
| 连接名 | `旅行项目-本地` |
| 主机 | `127.0.0.1` |
| 端口 | `15432` |
| 初始数据库 | `trip` |
| 用户名 | `trip_app` |
| 密码 | 从下面的本地凭据文件复制 |
| SSH 隧道 | 不启用 |

将以下路径粘贴到 Windows 文件资源管理器地址栏，用记事本打开：

```text
\\wsl.localhost\Ubuntu\home\kyle\dev\config\db-trip.json
```

找到 `"password"`，复制其后引号内的内容，不包含引号。

**初始数据库务必填写 `trip`，不要保留默认的 `postgres`**，因为应用账号没有访问本地管理库的权限。填写完成后点击 **“测试连接” → “确定”**。

---

**再连接远程服务器数据库。**

**Navicat 要经过两道连接，才能访问远程数据库。**

1. **先连接服务器**：使用公网 IP `8.163.48.116`、服务器账号 `root` 和密钥文件，建立一条加密通道。这些信息填在 **“SSH”页**。
2. **再连接数据库**：通过这条通道，访问服务器内部的数据库 `172.18.0.2:5432`，使用数据库账号 `trip_app` 和数据库密码。这些信息填在 **“常规”页**。

新建另一个 PostgreSQL 连接，在 **“常规”页**填写：

| 配置项 | 填写内容 |
|---|---|
| 连接名 | `旅行项目-线上` |
| 主机 | `172.18.0.2` |
| 端口 | `5432` |
| 初始数据库 | `trip` |
| 用户名 | `trip_app` |
| 密码 | 从下面的线上数据库密码文件复制 |

这里的 `172.18.0.2` 是我刚核对的**远程数据库容器地址**，已验证服务器能够访问。

线上数据库密码在你电脑上留有一份，我已核对与服务器一致。用记事本打开以下文件，复制其中整段内容：

```text
\\wsl.localhost\Ubuntu\home\kyle\dev\projects\langgraph-trip-planner\deploy\secrets\app_password
```

随后切换到 **“SSH”页**：

| 配置项 | 填写内容 |
|---|---|
| 使用 SSH 隧道 | 勾选 |
| 主机/IP 地址 | `8.163.48.116` |
| 端口 | `22` |
| 用户名 | `root` |
| 验证方法 | 公钥 / Public Key |
| 私钥文件 | 选择下方 `.pem` 文件 |
| 密钥口令 / Passphrase | 留空；当前密钥没有设置口令 |

私钥文件：

```text
C:\Users\LK\Desktop\Agent_Project\阿里云服务器\密钥文件\kyle-server-key.pem
```

然后点击 **“测试连接” → “确定”**。当前数据库没有配置数据库层 SSL，SSL 页保持未启用；公网传输由 SSH 隧道加密。

这里要区分三个身份：**`root` 是服务器账号，`trip_app` 是数据库账号，网页注册的邮箱是网站账号。** 它们的密码不能混用。

**这个方式不需要开放阿里云安全组的 5432 端口，也不需要修改线上容器配置。** 如果以后重建数据库容器，内部 IP 可能改变；可在服务器终端重新查询：

```bash
docker inspect --format '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}' trip-production-postgres-1
```

---

**连接成功后这样查看数据。**

双击连接，展开 **`trip → public → 表`**，可以看到：

| 表 | 内容 |
|---|---|
| `accounts` | 网站账号 |
| `trips` | 行程与结果 |
| `trip_runs` | 规划执行记录 |
| `trip_events` | 进度事件 |
| `daily_usage` | 每日使用额度 |

点击“新建查询”，先运行这段只读 SQL：

```sql
SELECT current_database(), current_user;

SELECT id, title, status, created_at
FROM public.trips
ORDER BY created_at DESC
LIMIT 20;
```

本地连接失败时，优先检查 Docker 和数据库是否启动；远程若出现 SSH 握手中断，检查本机代理是否将 `8.163.48.116` 设为直连——此前你的 Windows 代理影响过 SSH 连接。操作线上表之前，先确认左侧选中的是哪个连接。